@model dynamic

@{
    ViewBag.Title = "GetOrderHProducts";
}
<style>
    #myInput {
        background-position: 10px 10px;
        background-repeat: no-repeat;
        width: 100%;
        font-size: 16px;
        padding: 12px 20px 12px 40px;
        border: 1px solid #ddd;
        margin-bottom: 12px;
    }
</style>
<h2>Order History</h2>
<input type="text" id="myInput" onkeyup="myFunction()" placeholder="Search for order location.." title="Type in a name">
<table  class="table table-bordered"  id="myTable">
    <thead>
        <tr style="background-color:skyblue">
            <th>User Name</th>
            <th>Company Name</th>
            <th>Mobile Name</th>
            <th>Ram</th>
            <th>Storage</th>
            <th>Color</th>
            <th>Price</th>
            <th>Location</th>
            <th>Store Name</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var products in Model)
        {
        <tr>
            <td scope="row">@products.UserName</td>
            <td>@products.C_Name</td>
            <td>@products.M_Name</td>
            <td>@products.Ram</td>
            <td>@products.Storage</td>
            <td>@products.Color</td>
            <td>@products.Price</td>
            <td>@products.Locations</td>
            <td>@products.Store</td>

        </tr>
        }
    </tbody>
</table>

<script>
    function myFunction() {
        var input, filter, table, tr, td, i, txtValue;
        input = document.getElementById("myInput");
        filter = input.value.toUpperCase();
        table = document.getElementById("myTable");
        tr = table.getElementsByTagName("tr");
        for (i = 0; i < tr.length; i++) {
            td = tr[i].getElementsByTagName("td")[7];
            if (td) {
                txtValue = td.textContent || td.innerText;
                if (txtValue.toUpperCase().indexOf(filter) > -1) {
                    tr[i].style.display = "";
                } else {
                    tr[i].style.display = "none";
                }
            }
        }
    }
</script>
